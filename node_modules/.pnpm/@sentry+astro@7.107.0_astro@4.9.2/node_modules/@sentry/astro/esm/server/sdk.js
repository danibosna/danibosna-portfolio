import { applySdkMetadata } from '@sentry/core';
import { init as init$1, setTag } from '@sentry/node';

/**
 *
 * @param options
 */
function init(options) {
  applySdkMetadata(options, 'astro', ['astro', 'node']);

  init$1(options);

  setTag('runtime', 'node');
}

export { init };
//# sourceMappingURL=sdk.js.map
