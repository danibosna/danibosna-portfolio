const core = require('@sentry/core');
const node = require('@sentry/node');

/**
 *
 * @param options
 */
function init(options) {
  core.applySdkMetadata(options, 'astro', ['astro', 'node']);

  node.init(options);

  node.setTag('runtime', 'node');
}

exports.init = init;
//# sourceMappingURL=sdk.js.map
